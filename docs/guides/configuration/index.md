# Configuration

Guides for multisite setup, output API choice, and project configuration patterns.

##### [render() vs nodes() vs tree()](/guides/configuration/render-vs-nodes-vs-tree)

Pick the right output API for your template, custom markup, or JSON endpoint.

##### [Multisite menus](/guides/configuration/multisite-menus)

Enable menus per site, edit per-site URLs, and render the correct tree on each site.

##### [JSON import and export](/guides/configuration/json-import-export)

Export and import portable menu JSON between environments via the control panel or console.
