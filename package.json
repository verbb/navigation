{
  "name": "navigation-plugin-workspace",
  "private": true,
  "workspaces": [
    "docs",
    "src/web"
  ],
  "scripts": {
    "dev:plugin-docs": "npm run docs:dev --prefix docs",
    "docs:screenshots": "npm --prefix docs run docs:screenshots --",
    "dev:builder": "npm run dev -w @verbb/navigation-builder",
    "build:builder": "npm run build -w @verbb/navigation-builder"
  }
}
